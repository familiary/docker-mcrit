storage:
  dbPath: /data/db
  # WiredTiger defaults its cache to ~50% of RAM minus 1 GB; set this when MongoDB shares the host.
  # wiredTiger:
  #   engineConfig:
  #     cacheSizeGB: 4

net:
  bindIp: 0.0.0.0
  port: 27017
